# Mapbox (opcional) — la vista de mapa funciona sin token.
# Añade un token sólo si quieres sustituirla por tiles reales de Mapbox.
NEXT_PUBLIC_MAPBOX_TOKEN=

# Sentry (opcional en dev, recomendado en prod)
NEXT_PUBLIC_SENTRY_DSN=
